# 安全修正 · 语音资产下载的信任锚

> **优先级 P0，阻塞 T6 的一切后续工作。**
> 本文件修正 `IMPROVEMENT_TASKS.md` 第一轮 T6 中的一个设计错误。
> 现有下载器实现的工程质量没有问题，问题在于**我给出的信任模型**跨越了 `AGENTS.md` 第 6 条。
>
> **本文件为第二版**，采用比第一版更简单也更根本的修法。若已看过第一版
> （主张"代码与数据分离"），请以本版为准：那个方案仍然有效，但它解决的是症状，
> 下面的方案解决的是病因，而且成本更低。

---

## 1. 问题：这是我的任务书写错了

第一轮 `IMPROVEMENT_TASKS.md` 的 T6 里我写了：

> 源地址写进一个**可远程更新的 JSON 清单**（版本号 + 每个档位的 **SHA256** + 多个 URL），
> 这样换源不需要发新版本
>
> 不要把下载源硬编码进代码——必须走可更新的 JSON 清单

同时 G4 又写着"**不得新增远程代码执行**……能力"。**这两条直接冲突，而我没有察觉。**

`voice-runtime` 档的 ZIP 里装的是 `python/python.exe`、大量 `.pyd`、
以及会被 `spawn` 执行的 `ireina_tts_service.py`。

**执行者停下来是正确的行为，不要绕过它继续。**

---

## 2. 病因不是"下载了代码"，是信任锚放错了地方

请注意这句话，它是本次修正的全部：

> **校验依据（SHA256）和被校验物（ZIP）来自同一个不可信源。**

清单里同时写着 `urls` 和 `sha256`。谁控制了清单，谁就能**同时改这两样**——
改一个就能配平另一个。于是 SHA256 校验退化成了"我下到的东西没在传输中损坏"，
而不是"这是作者发布的那个东西"。

现有的所有防护都拦不住这一点，因为它们保护的是**传输与安装的完整性**，
不是**发布者的真实性**：

| 现有措施                                | 防住了什么         | 防不住什么                               |
| --------------------------------------- | ------------------ | ---------------------------------------- |
| 强制 HTTPS                              | 中间人篡改传输     | 源站本身被控制                           |
| SHA256 校验                             | 传输损坏、部分投毒 | 哈希和资产由同一方提供                   |
| ZIP Slip / 符号链接 / 文件数 / 体积校验 | 路径穿越、解压炸弹 | ZIP 里放的就是合法路径下的恶意可执行文件 |
| staging + 原子替换                      | 半成品污染         | 替换进去的本来就是恶意内容               |
| 清单 URL 只由 Main 进程环境变量提供     | Renderer 提权      | 环境变量指向的那个站点被控制             |

一旦看清病因，修法就很直接：**把信任锚从远程挪回应用本身。**

### 风险目前尚未激活

改动未提交、未打包，`FPNF_SPEECH_ASSET_MANIFEST_URL` 未设置，
没有任何远程内容被下载或执行过。**这是设计缺陷，不是事故。现在改成本最低。**

---

## 3. 采纳方案：SHA256 硬编码进应用，URL 仍可远程更新

### 核心规则

| 项                               | 放在哪                         | 能不能远程改 |
| -------------------------------- | ------------------------------ | ------------ |
| **每个档位的 SHA256**            | **应用源码里，随应用一起分发** | **绝对不能** |
| 每个档位的解压后体积、文件数上限 | 应用源码里                     | 不能         |
| 档位 id 的合法集合               | 应用源码里（固定枚举）         | 不能         |
| 下载 URL（主源 / 备源 / 兜底）   | 远程清单，或也硬编码           | **可以**     |

**远程清单降级为"从哪下"的路由表，不再有权决定"下什么"。**

### 为什么这样就够了

攻击者完全控制了托管站点和清单之后，能做的上限是：

- 提供一个哈希对不上的文件 → 客户端拒绝 → **下载失败**
- 让所有 URL 都不可达 → **下载失败**

也就是**只能拒绝服务，无法投毒**。他改不了应用二进制里的那个哈希，
而应用是用户从官方 Release 下载、已经信任的东西。

这跟 Linux 发行版的信任模型是同一个思路：apt 的包哈希写在签名过的 Release 文件里，
信任锚是**预装的**公钥。硬编码哈希是同一思路的简化版——信任锚就是应用本体，
省掉了整套密钥管理。

**结论：这不再是"新增远程代码执行能力"，`AGENTS.md` 第 6 条不再被触碰。**
被下载的即使是 `python.exe`，它也必须逐字节等于作者发布时算出的那一个。

### 代价

**换资产必须发新版应用。** 想换音色、更新模型、修 Python 环境里的包，
都要改源码里的哈希然后发版。

对这个项目这是可以接受的，甚至是**正确的**：

- 模型和运行时不常变
- 它们真变了，代码往往也要配合改（比如换 BERT 尺寸要改加载路径）
- 而且"资产能被静默换掉"本身就不是一个应该拥有的能力

**换源不受影响**——URL 仍在清单里，HuggingFace 挂了改清单就能切 R2，不用发版。

---

## 4. 具体改法

### 4.1 新增一个不可远程覆盖的哈希表

新建 `src/main/speech/speech-asset-integrity.ts`（或放进现有模块，但要单独成块）：

```ts
/**
 * 语音资产的信任锚。
 *
 * 这些值随应用一起分发，是判断"下载到的东西是不是作者发布的那一个"的唯一依据。
 * 远程清单只能决定从哪里下载，绝不能决定下载什么。
 *
 * 更换任何一档资产都必须：改这里的 sha256 → 发布新版本应用。
 * 任何让这张表变得可以远程覆盖的改动都是安全回退，不得实施。
 */
export const SPEECH_ASSET_INTEGRITY = {
  'voice-runtime': {
    version: '1.0.0',
    sha256: '<64 位小写十六进制>',
    compressedBytes: 0, // 打包后回填真实值
    extractedBytes: 0, // 打包后回填真实值
    maxEntries: 0, // 打包后回填真实值
  },
  'speech-input': {
    version: '1.0.0',
    sha256: '<64 位小写十六进制>',
    compressedBytes: 0,
    extractedBytes: 0,
    maxEntries: 0,
  },
} as const;

export type SpeechAssetTierId = keyof typeof SPEECH_ASSET_INTEGRITY;
```

### 4.2 清单 schema 改为只含 URL

升 `schemaVersion` 到 **2**，让旧清单被明确拒绝：

```json
{
  "schemaVersion": 2,
  "tiers": [
    {
      "id": "voice-runtime",
      "version": "1.0.0",
      "urls": ["https://主源/...zip", "https://备源/...zip"]
    },
    {
      "id": "speech-input",
      "version": "1.0.0",
      "urls": ["https://主源/...zip", "https://备源/...zip"]
    }
  ]
}
```

**清单里不再出现 `sha256`，也不再出现 `bytes`。**
若解析到 `sha256` 字段，按"额外字段"直接拒绝整份清单——
现有的严格 JSON 校验已经会这么做，确认它对新 schema 仍然生效。

校验逻辑改为：

- `id` 必须在 `SPEECH_ASSET_INTEGRITY` 的键集合内，未知 id **拒绝整份清单**
- `version` 必须与 `SPEECH_ASSET_INTEGRITY[id].version **严格相等**，
  不匹配则该档不可用（这同时挡住了降级攻击：攻击者无法诱导客户端去下旧版本资产）
- `urls` 沿用现有的 HTTPS 强制、去重、无凭据、无 fragment 校验

### 4.3 下载与校验路径

- 声明体积、Content-Range 一致性检查，改为对照 `SPEECH_ASSET_INTEGRITY` 里的
  `compressedBytes`，**不再对照清单**
- 合并后的 SHA256 比对，改为对照 `SPEECH_ASSET_INTEGRITY[id].sha256`
- 解压时的文件数、解压体积上限，改为对照 `extractedBytes` / `maxEntries`
- **磁盘空间检查**（第二轮 R2 要求的那条）现在有了可靠依据：
  开始下载前用 `extractedBytes × 1.3 + compressedBytes + 500 MB` 做阈值

### 4.4 如果暂时没有托管站点

现在 `FPNF_SPEECH_ASSET_MANIFEST_URL` 未设置。在拿到正式托管之前，
可以让 URL 也直接硬编码在 `SPEECH_ASSET_INTEGRITY` 旁边，
清单机制作为"可选的 URL 覆盖"存在：**未设置环境变量时用内置 URL，
设置了就用清单里的 URL 换源**。这样安全性完全不变，
而且在没有站点的当下也能跑通完整链路（配合本地静态服务器测试）。

---

## 5. 验收标准

- [ ] **构造一个"清单提供不同 sha256"的测试** —— 断言该字段被当作额外字段拒绝，
      且下载仍然只按内置哈希校验
- [ ] **构造一个"内容与内置哈希不符"的 ZIP** —— 断言整包拒绝、staging 被清理、
      给出可读的中文错误，且**不会**因为多个 URL 而反复重试同一个坏内容
- [ ] **构造一个"清单声明未知 tier id"的用例** —— 断言整份清单被拒绝
- [ ] **构造一个"清单 version 与内置 version 不一致"的用例** —— 断言该档不可用
- [ ] `schemaVersion: 1` 的清单被拒绝
- [ ] 全部 URL 不可达时安全失败，文字聊天与 Live2D 不受影响
- [ ] 磁盘空间不足时在**下载开始前**给出提示并停止
- [ ] 代码中**不存在任何**能让 `SPEECH_ASSET_INTEGRITY` 被运行时覆盖的路径
      （不从文件读、不从环境变量读、不从 IPC 读）
- [ ] `docs/SPEECH_ASSET_DOWNLOADS.md` 更新，写清楚新的信任模型
- [ ] 现有的 HTTPS 强制、ZIP Slip、符号链接、staging 原子激活、
      Range 并发、断点续传、多源回退**全部保留**
- [ ] `pnpm verify` 通过

---

## 6. 可选加固（做完第 3 节之后，按需选择）

以下三条都**不是必需的**了。信任锚已经归位，它们是纵深防御。

### 6.1 代码与数据分离

把 Python 运行时打进安装包，下载包里只放 ONNX 权重与配置。

- 成本：主包 +45 MB（145 → 190 MB），需要把 BERT 从
  `python/Lib/site-packages/bert/` 挪到独立数据目录，并给
  `ireina_tts_service.py` 加一个 BERT 路径环境变量
  （参照已有的 `FPNF_BUNDLED_VOICE_ROOT`；需先确认
  `onnx_bert_models.load_model()` 是否接受自定义路径）
- 收益：万一将来哈希更新流程出错，攻击面上限也只是畸形 ONNX，不是可执行文件
- **建议：现在不做，记录为待办。** 等 T6 完整跑通、体积数据落地后再评估

### 6.2 解压时的可执行扩展名检查

即便保持现在的打包方式，也可以在解压时记录 ZIP 内的可执行文件清单，
与一份内置的预期清单比对，不符就拒绝。

- 这是一条**廉价的机器可验证约束**，比文档承诺硬
- 如果做了 6.1，这条就退化成"白名单只允许 `.onnx/.json/.npy/.txt/.bin`"，非常干净

### 6.3 清单签名

Ed25519 签名清单、公钥硬编码。

- 唯一优势：**换资产不用发新版应用**
- 代价：私钥管理。对个人项目而言，私钥泄露的风险大于这点便利
- **建议：不做。** 硬编码哈希已经提供了同等的安全性，且没有密钥可丢

---

## 7. 三种修法的对比（供决策留档）

|                  | **A · 哈希硬编码**（采纳） | B · 代码数据分离 | C · 清单签名 |
| ---------------- | -------------------------- | ---------------- | ------------ |
| 消除远程代码执行 | ✅                         | ✅               | ✅           |
| 主包体积         | **不变（145 MB）**         | +45 MB           | 不变         |
| 换源是否需发版   | 否                         | 否               | 否           |
| 换资产是否需发版 | **是**                     | 是               | 否           |
| 需要密钥管理     | **否**                     | 否               | **是**       |
| 实现成本         | **最低**                   | 中               | 中           |
| 抗托管站点沦陷   | ✅ 只能 DoS                | ✅ 只能投毒数据  | ✅ 只能 DoS  |
| 抗私钥泄露       | **不适用（无私钥）**       | 不适用           | ❌           |

A 与 B 可以叠加，A 是根本修复，B 是纵深防御。C 对个人项目不划算。

---

## 8. 给执行者的话

上一轮在这件事上**停下来是完全正确的**，`AGENTS.md` 第 6 条和
"需求冲突时停下并说明"这两条都被正确执行了。不要因为收到本文件
就认为之前的判断有误——是任务书需要修正，不是实现。

现有下载器的实现（Range 并发、断点续传、多源回退、严格 JSON 校验、
ZIP Slip 防护、staging 原子激活、竞态处理）**质量很高，全部保留**。
本次只改一件事：**校验依据从哪里来。**

**仍然：不要提交、推送、发布、打 tag、上传任何产物。**
本地 `pnpm package:win` 做验收是允许的（见第二轮文件第 0 节对 G8 的澄清）。
