# 交接报告 · 致继续本项目的编码助手

> 2026-09-04 最新：资源中心四类七项组件、统一卡片说明、Genie 句尾修复和公开组件发布已完成。
> 主程序开发版本 1.8.0，七个编译期记录均为实测值；正式目录和下载路由已接入。
> 用户最新要求：先完成全部 v1.8 任务及验收，再打包推送。主程序尚未提交、推送或发布；见 `docs/V1_8_DELIVERY.md`。
> 下方是原交接基线，其中旧的 null 记录、两档方案、未授权发布描述不代表当前状态。

**项目**：for-people-no-friend（FPNF），Electron + TypeScript 的 Windows Live2D 桌宠伴侣
**仓库**：`C:\ai_deskpet`
**原交接基线版本**：v1.7.0
**这份文件的作用**：让你在不重读全部历史的情况下知道现在的状态、边界和下一步。
细节规格在另外三份文件里，本文件末尾有导航。

---

## 1. 先说那次安全暂停：你的判断是对的

你在实现语音资产按需下载时停了下来，理由是它跨越了 `AGENTS.md` 第 6 条
（不得新增远程代码执行能力）。

**这个判断准确，停下来是正确行为。不要因为收到新指令就认为之前判断错了。**

问题不在你的实现——下载器的工程质量很高（Range 分段并发、断点续传、多源回退、
严格 JSON 校验、ZIP Slip 防护、符号链接校验、staging 原子激活、取消与完成的竞态处理），
这些**全部保留**。

问题在**任务书给的设计**。第一轮 `IMPROVEMENT_TASKS.md` 的 T6 要求
"把 SHA256 和 URL 一起写进可远程更新的清单"，同时全局约束 G4 又禁止新增远程代码执行能力。
这两条互相冲突，写任务书的一方没有察觉。**任务书已修正，见第 3 节。**

如果你看到过一版主张"把 Python 打进主包、只下载 ONNX 权重"的修正说明，
那一版已被更简单的方案取代，以 `SECURITY_FIX_SPEECH_ASSETS.md` 现在的内容为准。

---

## 2. 当前项目状态

### 已完成（第一轮 14 项中的 10 项）

|                   | 结果                                                        |
| ----------------- | ----------------------------------------------------------- |
| T1 流式回复 DOM   | delta 只更新单一回复节点；滚动、结束、取消、清理都有测试    |
| T4 SecretStore    | 写入串行化、坏条目跳过、并发回归测试                        |
| T5 IPC 守卫       | sender 校验机制化，注册参数改为对象                         |
| T9 CSP            | 生产 CSP 去掉开发 WebSocket，补齐 base-uri / form-action 等 |
| T10 SQLite        | 裁剪节流、busy_timeout、2100→2000 回归                      |
| T11 审批脚本原文  | 审批前显示实际脚本，长度限制，纯文本渲染                    |
| T12 声明顺序      | devServerUrl 顺序修正                                       |
| T13 诊断日志      | 五类关键降级路径接入脱敏诊断                                |
| T14 SQLite backup | Electron 43 / Node 24.18.1 实测 backup 可用，有降级测试     |
| T8 打包白名单     | 代码层完成，**未执行正式打包验收**                          |

### 核心迁移完成

- **T2 ASR 去 Python**：`local-sherpa-asr.ts` 直接加载 SenseVoice，
  Python/FastAPI/uvicorn ASR 服务与 9880 探测已删除。
  Electron 43 实测 addon 可加载，修复了 sherpa-onnx-node 动态导入时接口在 `default` 下的问题。
  连续转写 20 次只创建一个 recognizer。远程 OpenAI 兼容 ASR 路径保留。
- **T3 干净 TTS 环境**：requirements + lock + 可重复构建脚本；
  重建后 801.3 MiB，旧的 1003.3 MiB，**减少 202 MiB**；CPU 与 DirectML 两条路径都实测出过 WAV。

### 部分完成

- **T6 分档下载**：下载器、管理器、IPC、UI、自动下载、麦克风触发、计费网络处理都已实现。
  **卡在信任模型上（第 3 节），且没有正式托管站点。**
- **T7 拆 chat-controller**：已拆出 `timeline.ts`、`composer.ts`、语音资产面板。
  `chat-controller.ts` 从 5824 行降到 5720 行。剩余五块见第 4 节。

### 一个改变了设计的实测结论

**删除日语 BERT 后 Style-Bert-VITS2 会直接失败，不会退化到平语调。**
所以 BERT 必须和 TTS 同档分发，不能做成可选第二档。分档因此从三档变两档，
第一档从约 240 MB 涨到约 490 MB。

### 验证基线

112 个测试文件、536 项测试；代码检查、格式检查、两套 TypeScript 检查、
Electron/Main 构建、Renderer 生产构建、沙箱 Preload 检查全部通过。
**这条基线不得下降。**

---

## 3. 修正后的信任模型（本次要做的核心改动）

### 病因

> **校验依据（SHA256）和被校验物（ZIP）来自同一个不可信源。**

清单里同时写着 `urls` 和 `sha256`，控制清单的人可以同时改这两样，改一个配平另一个。
于是 SHA256 只证明"传输没损坏"，不证明"这是作者发布的那一个"。
HTTPS、ZIP Slip 防护、staging 原子替换保护的都是**传输与安装的完整性**，
建立不了**发布者的真实性**。

### 修法：把信任锚从远程挪回应用本身

| 项                         | 放在哪                   | 能否远程改   |
| -------------------------- | ------------------------ | ------------ |
| 每档的 SHA256              | **应用源码，随应用分发** | **绝对不能** |
| 每档的解压体积、文件数上限 | 应用源码                 | 不能         |
| 合法档位 id 的集合         | 应用源码（固定枚举）     | 不能         |
| 下载 URL（主源/备源/兜底） | 远程清单                 | 可以         |

**远程清单降级为"从哪下"的路由表，无权决定"下什么"。**

攻击者完全控制托管站点后，能做的上限是**拒绝服务**——
提供哈希对不上的文件会被拒绝，他改不了应用二进制里的哈希。

这是 apt 信任模型的简化版：信任锚从"签名的 Release 文件 + 预装公钥"
简化为"应用本体"，省掉整套密钥管理。

**代价**：换资产（换音色、更新模型、修 Python 环境）必须发新版应用。
**换源不受影响**——URL 仍在清单里，可随时切换。

**结论：改完之后不再是"新增远程代码执行能力"，`AGENTS.md` 第 6 条不再被触碰。**

### 不要做的三件事

- **不要**做清单签名（Ed25519 那套）。硬编码哈希已提供同等安全性，且没有私钥可丢。
- **不要**为此把 Python 运行时打进主包。那是可选加固，现在不做，记为待办即可。
- **不要**保留任何能让内置哈希表被运行时覆盖的路径（不从文件读、不从环境变量读、不从 IPC 读）。

具体改法与验收标准见 `SECURITY_FIX_SPEECH_ASSETS.md` 第 4、5 节。

---

## 4. 现在按这个顺序做

| 顺序  | 任务                                                                                                          | 规格在哪                             | 预估   |
| ----- | ------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ------ |
| **1** | **信任模型修正**（哈希硬编码、清单 schema 升 2）                                                              | `SECURITY_FIX_SPEECH_ASSETS.md` §4–5 | 半天   |
| **2** | **本地打包验收**（T2 addon 加载、T8 裁剪副作用、T3 打包后 TTS、T6 真实体积）                                  | `IMPROVEMENT_TASKS_ROUND2.md` R1     | 半天   |
| **3** | **T6 分档修正 + 磁盘空间检查**                                                                                | `IMPROVEMENT_TASKS_ROUND2.md` R2     | 半天   |
| **4** | **T6 故障注入验收**（断网、强杀进程、磁盘写满、SHA 不符、全源不可达）                                         | `IMPROVEMENT_TASKS_ROUND2.md` R4     | 半天   |
| **5** | **T7 剩余五个面板**：memory-panel → settings-provider → settings-speech → settings-vtube → settings-character | `IMPROVEMENT_TASKS_ROUND2.md` R5     | 持续   |
| **6** | **Genie-TTS 端到端听感验收**                                                                                  | `IMPROVEMENT_TASKS_ROUND2.md` R6     | 1 小时 |

**第 2 步里有一条最容易出事**：T8 删掉了 `dxcompiler.dll` / `dxil.dll`，
必须在**有独立 GPU 和无独立 GPU（或强制软渲染）两种情况下**分别启动，
确认 Live2D 都能正常显示。

**托管与 Ireina 音色的再分发许可需要作者本人决策**，不是你的任务范围，
在拿到之前不要虚构任何 URL，不要把私密资产放进 Git、安装包或公开 Release。
可以用本地静态文件服务器跑通完整下载链路做验收——这不需要公网站点。

---

## 5. 边界

### 全局约束

| #   | 约束                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| G1  | **不得改动任何 ONNX 模型权重**。已按音质优先排除量化，三个模型原样保留                                                                                                                                             |
| G2  | **保持 zip 免安装分发**，不引入 NSIS / MSI / 代码签名                                                                                                                                                              |
| G3  | **不得削弱 Electron 信任边界**：`contextIsolation` / `sandbox` / `nodeIntegration:false` / preload 白名单一律不动                                                                                                  |
| G4  | **不得新增远程代码执行、任意文件访问、屏幕观察、桌面控制能力**                                                                                                                                                     |
| G5  | **可选子系统失败必须安全降级**，不得阻塞纯文字聊天与本地角色显示                                                                                                                                                   |
| G6  | **一次只做一个任务，单独提交**（提交指本地 staging，见 G8）                                                                                                                                                        |
| G7  | 完成后跑最小相关测试；跨边界改动跑 `pnpm verify`                                                                                                                                                                   |
| G8  | **不得 `git commit` / `git push` / 创建 Release / 打 tag / 改写历史 / 修改仓库可见性 / 上传任何产物**。<br>**允许并要求**：本地 `pnpm package:win` 产出 `release/win-unpacked/` 做验收，验完可删。这是验证不是发布 |

冲突时停下来说明，不要自行取舍——这一点你上次做对了。

### 已经做对、不要"顺手优化"的部分

- Electron 安全配置：四道锁（`setWindowOpenHandler` 拒绝、`will-navigate` 阻止、
  `will-attach-webview` 阻止、sandbox）
- 麦克风权限收窄：只有主窗口、只有 `media`、只有单一 `audio` 类型、只有 `file:` 或 dev server 源
- preload 白名单：无 `ipcRenderer.invoke` 透传，Main→Renderer 事件全过 `parseXxxEvent` 校验
- 渲染层**零 `innerHTML`**：全部 `createElement` + `textContent`。
  **不要为了方便引入任何 HTML 字符串拼接**
- 所有网络适配器的 `AbortSignal.any([外部信号, AbortSignal.timeout()])` 双重取消
- `project-check-runner` 的子进程隔离：env 白名单 12 个变量 + 强制 `CI=1` + `shell:false`
- 命名空间隔离：人设、记忆、术语表、知识全部按 `memoryNamespace` 过滤
- **112 个测试文件是本项目最值钱的资产**，任何改动不得净减少测试覆盖

---

## 6. 文件导航

| 文件                               | 内容                                                                                   |
| ---------------------------------- | -------------------------------------------------------------------------------------- |
| `AGENTS.md`                        | 仓库强制规则，**每次动手前先读**                                                       |
| `docs/SECURITY_CODING_STANDARD.md` | 安全编码标准，**每次动手前先读**                                                       |
| `SECURITY_FIX_SPEECH_ASSETS.md`    | 信任模型修正的完整规格（本次第 1 步）                                                  |
| `IMPROVEMENT_TASKS_ROUND2.md`      | R1–R7 剩余任务规格；第 0 节有 G8 的澄清                                                |
| `IMPROVEMENT_TASKS.md`             | 第一轮 14 项的完整规格；附录 A 记录了已排除的方案及原因，附录 B 是"不要顺手优化"的清单 |
| `docs/SPEECH_ASSET_DOWNLOADS.md`   | 下载机制的现状文档，**信任模型改完后需同步更新**                                       |

`IMPROVEMENT_TASKS.md` 里有两处已被后续实测推翻，以 `IMPROVEMENT_TASKS_ROUND2.md`
附录和 `SECURITY_FIX_SPEECH_ASSETS.md` 为准：

1. "BERT 只影响韵律、缺失时会退化到平语调" → **错，缺 BERT 直接失败**
2. "把 SHA256 写进可远程更新的清单" → **错，SHA256 必须硬编码**
