# V1.9 人工验收清单

历史开发基线见 [v1.8.3 交付记录](V1_8_3_DELIVERY.md)，当前发布结果见 [V1.9 交付记录](V1_9_DELIVERY.md)。下文是逐次验收历史，后续记录补充此前尚未验证的项目。以下项目需要人工观察或试听，不能以自动测试代替。

## 2026-09-13 验收记录

- 使用者反馈：“都没问题 语音本身”。语音本身按使用者实际试听确认记录；不据此推定连续切换、离页停止、长短句打断等每个步骤均已单独完成。
- 本次 Windows 原生控制通过 `node_repl` 的 `@oai/sky` 成功列出并观察既有桌宠窗口，随后实际打开独立资源中心；不是仅凭进程存在判断界面正常。资源中心显示 10 项资源、0 项已安装，四种音色有试听入口和运行方式说明。Mika、菲比实际点击后出现“停止试听”状态；没有取得系统声音采集证据，不单凭按钮变化判定音频输出或互斥通过。
- 时钟在主窗口实际可见，多次观察时间有变化，显示 9 月 13 日、Australia/Sydney。它在验收开始时已启用，本次未验证首次安装默认关闭、启停、重启保持或点击穿透。
- 实机发现展开菲比来源说明后，安装按钮纵向拉伸、台词挤成窄列；收起后恢复。原因是安装按钮、台词与长说明共用 flex 操作行。已将试听与来源说明移到卡片内独立区域，保留原有播放和下载行为。
- 隔离浏览器使用现有资源中心预览页、真实渲染代码与模拟状态复现并验证修复：4 个宽度（360、440、700、1066 CSS 像素）× 说明展开/收起 × 4 种音色均通过按钮高度、台词宽度和卡片溢出检查。1066 像素下，菲比展开说明时安装按钮由修复前 280 像素恢复为 30 像素，台词宽度由 24 像素恢复为 850 像素。该检查不等同 Windows DPI 验收。
- 复现脚本、前后测量和模拟页面截图保存在本地 `.release/v1.9-ui-acceptance-20260913/`；命令为 `node .release/v1.9-ui-acceptance-20260913/browser-layout-check.mjs after`。资源中心及试听相关测试共 9 项通过，两套类型检查、针对改动的 ESLint/Prettier、`git diff --check` 及输出到上述隔离目录的 Renderer 构建均通过。本次未重跑历史完整 `pnpm verify`，也未覆盖正在使用的构建产物。
- 工具随后检测到使用者正在操作资源窗口，已停止桌面输入。没有重启正在使用的主程序，因此修复后的运行中窗口仍待复看。资源窗口最小化/恢复、下载中关闭重开、VTS/Spout、ViewerEX、原生 Live2D、多屏/DPI 和性能项目仍未在本次完成。

### 同日继续验收

- 使用者要求继续后，重新选择了当前窗口。资源中心实际最小化后，工具确认窗口处于最小化状态；从桌宠设置再次打开时恢复同一窗口，并保留音色分类和滚动位置。随后实际关闭资源中心，运行 `pnpm build:renderer` 成功，再重开新窗口；资源目录仍显示 10 项、0 项已安装。展开 Mika 来源说明后，安装按钮保持正常高度，试听台词与来源全文正常排列。布局修复已在实际资源窗口复看；没有下载中关闭重开的实机证据。
- 时钟实际关闭后主窗口时钟消失；关闭设置再经“小组件”入口重开，仍显示“已关闭”。重新启用后显示“运行中”，回到桌面再次出现日期、Australia/Sydney 和变化的秒数。最终恢复为原有启用状态。这里验证的是设置页重开保持，不是主程序重启保持；首次安装默认关闭和点击穿透仍未验证。
- 经桌宠已有按钮启动 VTube Studio，现有模型成功加载；点击连接后 API 已连接，能读取模型与表情。VTS 的 Spout2 开关原本已开，曾关闭再开启作恢复检查。设置面板展开期间桌宠保留旧的“无 Spout2 输出”诊断，源码确认该期间接收层会暂停；关闭设置后模型实际出现在桌宠角色区域，`FpnfVTubeStudioSpout` 接收进程有响应。没有据此确认持续接收故障，也没有改动传输代码。
- 对真实模型执行 Param100 单项“预览并返回桌面”和“关闭预览”，操作后均返回桌面且模型继续显示；仅凭这些截图不认定表情变化幅度或自然度合格。内置“测试惊讶表情”返回“VTube Studio 表情联动测试通过”。源码显示该按钮在 3 秒后发送中性恢复，不能替代十通道混合、渐变和 8 秒恢复的实机验收。当前模型的表情映射尚未确认，本次没有替使用者保存候选映射。
- ViewerEX 当前禁用，状态动作、表情编号、情绪候选、角色动作四类映射均为空（显示示例占位文案）。本机安装目录存在，使用其 launcher 启动后出现 `lw.exe` 的 Windows 防火墙网络访问提示。未操作该系统安全权限请求，桌面输入已暂停等待使用者处理。ViewerEX 连接、候选轮换和清理，原生 Live2D，以及主程序重启、多屏/DPI、性能等验收仍未完成。
- 使用者随后自行同意防火墙提示。重新确认 ViewerEX 的 `lw.exe` 在运行，10086 端口由该进程监听；按源码核对 `/api` 路径后，`ws://127.0.0.1:10086/api` WebSocket 握手成功，未发送模型操作命令。根路径不接受该握手，不作为应用缺陷。此前“ViewerEX 禁用”仅指 FPNF 内的显示适配开关，不代表 ViewerEX 程序已退出或未安装。打开其控制面板后可见本机资源“魔女之旅伊蕾娜 v0.1”；没有把资源列表中的缩略图当作已加载模型或动作映射的证明。FPNF 的 ViewerEX 开关及空映射仍未改动。
- 使用者整理窗口后，实际启用并保存 FPNF 的 ViewerEX 显示模式，点击“发送本机测试气泡”，界面返回“测试气泡已发送到本机 ViewerEX”。该反馈只证明本机 WebSocket 已发送消息，服务未等待 ViewerEX 执行回执；没有取得角色旁气泡实际出现的画面证据。四类映射保持为空，因此候选轮换、动作衔接和表达清理仍不记为通过。
- 随后实际启用并保存纯 Live2D，关闭设置后显示“无法读取本地 Live2D 模型配置”，文字聊天、媒体和时钟仍可见。源码确认这是读取模型清单失败的提示；开发目录中的 `assets/models/local/model.json` 存在，但当前构建的 `dist/renderer/models/local/model.json` 不存在。Vite 明确使用 `publicDir: false`，要求运行时从已验证角色包存储加载模型，不会隐式复制开发者模型。未复制私人资源、导入替代模型或把缺少模型条件判为 GPU 渲染故障；本次纯 Live2D 显示、情绪和 GPU 验收未通过。
- 显示模式验收后已重新启用并保存 VTube Studio，关闭设置后现有模型重新出现在桌宠角色区域，并观察到眼睛状态变化；时钟继续更新时间。最终恢复 VTS 启用、鼠标追踪启用、时钟启用，ViewerEX 与纯 Live2D 的 FPNF 显示开关关闭。ViewerEX 外部程序仍在运行，未把关闭集成开关当作退出外部程序。未重启主程序，也未执行提交、推送、打包或发布。

### 同日并行回归与缺陷修复

- 实机缺失模型提示的根因已区分：Main 没有返回当前角色的模型清单时，Renderer 仍尝试读取开发目录回退路径，导致“未导入”被误报为“读取失败”。现在该场景直接显示“当前角色尚未导入 Live2D 模型”，并指向设置中的模型或角色包导入入口；已导入模型的传输错误仍保留为不可用。新增回归先出现 1 项失败、10 项通过，修复后模型清单、运行时兼容与导入服务共 28 项通过。隔离浏览器验证真实错误面板和重试按钮通过；页面重载后，实际桌宠也已出现新提示。未导入替代模型，不能将此提示修复等同于模型渲染验收。
- VTS 并行检查复现旧 idle IPC 晚于新 thinking 或新回复返回时，重新安排 8 秒 neutral 的竞态。新增修订号核对，并在显示模式切换和页面卸载时销毁恢复计时器；销毁后的实例可重新使用。4 项新增行为回归覆盖旧 idle、后续回复、销毁与重用；VTS client、ViewerEX client 和设置接线共 46 项通过。原始假 API 探针重跑不再产生越期 neutral。该结果不替代真实模型的自然度和打断视觉验收。
- 小组件并行验证：既有 7 文件、47 项测试通过；隔离真实安装服务与临时存储探针另 1 项通过。首次安装默认关闭；启用后重建 Runtime、Store、Service 保持启用；关闭后再次重建保持关闭。安装使用生产信任锚与 mock 下载。真实 Electron 进程重启、点击穿透、多屏/DPI 和退出后的计时器数量仍未由该探针覆盖。证据位于本地 `.release/v1.9-ui-acceptance-20260913/widget-validation/`。
- 资源中心与试听并行验证：既有 4 文件、12 项测试和 7 项隔离生命周期探针通过，覆盖旧播放回调不干扰新试听、停止与分类离页、销毁后迟到状态、窗口入口卸载清理与新挂载的下载快照。fake audio、模拟 DOM 和本地 HTTP 不等同耳听或真实窗口活动下载中关闭重开。证据位于本地 `.release/v1.9-ui-acceptance-20260913/voice-resource-validation/`。
- 本轮实际重新运行完整 `pnpm verify` 通过：156 个测试文件、849 项测试，以及 lint、格式、两套类型检查、Electron/Preload/Renderer 构建、沙箱 Preload、原生 sherpa、SQLite 备份与首次/已有配置启动检查。以上测试数不含 `.release` 中的隔离探针。未打包、提交、推送或发布。
- 随后的实机复看发现无模型错误卡被聊天面板遮住：`.character-host` 未建立定位包含块，使错误卡按全窗口居中。只增加 `position: relative` 后，真实 CSS 与错误面板的 16 种尺寸、左右位置和小组件组合由全红转为全绿；957×661 下与聊天重叠从 146 像素降至 0。针对最终 CSS 通过格式检查、`git diff --check` 和 Renderer 构建，未为这一行布局改动重复完整测试。重新加载实际桌宠后，错误卡在右侧角色区域完整显示，导入指引及“重新加载”按钮可见且可点击，点击后仍正确提示未导入。完成后恢复 VTS 与时钟启用；本次仅重载页面，未重启主进程。

### 同日剩余实机项目复查

- 使用者明确选择继续剩余实机验收。ViewerEX 控制面板中双击现有伊蕾娜模型后出现“正在请求”并返回列表；随后点击模型自定义入口，实际显示“无可自定义内容”。未取得动作组、动作名或表情编号，未把列表顺序当作 ExAPI ID，也未写入猜测映射。该反馈不证明模型没有任何内建动作，只说明当前自定义入口未提供验收所需内容。ViewerEX 编辑浮窗仍覆盖部分其他窗口，未修改其中位置或缩放数值。
- 纯 Live2D 的额外运行前提已核实：本机 `assets/models/local/live2dcubismcore.min.js` 存在，但构建目录 `dist/renderer/models/local/live2dcubismcore.min.js` 不存在；已导入模型的 `coreUrl` 仍指向这个构建相对路径，模型导入不会复制 Core。仅导入模型不能补齐当前验收环境。需显式准备受控的本地 Core 和有效模型，而不是复制整个开发资产目录。没有活动角色模型已由上一轮新提示在实际窗口确认；不能仅依据 dist 清单缺失来判断角色包状态。
- 实际资源中心完成了一段纯键盘流程：Tab 聚焦搜索框，输入 Mika；依次经过状态筛选、刷新目录和分类按钮，用 Enter 打开音色分类；继续 Tab 到安装和试听按钮。焦点边框可见，未触发安装；Enter 与空格均能触发“停止试听”状态。由于短音频可能在后续操作前自然结束，不以连续按键记录认定主动停止已完成。
- 后续停止试听输入被工具以“检测到使用者正在操作此窗口”拒绝，已停止桌面输入。该次主动停止、播放中离页及关闭后的无残留声音仍未通过；保留原先自动生命周期验证和使用者对语音本身的耳听反馈作为不同层级的证据。本轮只追加验收记录，没有修改生产代码或进行打包、提交、推送、发布。
- 并行完成纯 Live2D 已知本地资源预检：16 个文件、1,855,547 字节，模型 JSON、MOC3、纹理、物理及 9 个 Motion 文件引用完整，3 个状态和 7 个动作映射均能对应真实组名及索引。仅测量 Core，未执行、复制或把实测哈希当作来源/许可证明。预检位于本地 `.release/v1.9-ui-acceptance-20260913/live2d-preflight/`。后续可新建独立验收角色导入模型，避免覆盖小猫；普通 `.model3.json` 导入生成空映射，仍不能替代完整表达验收。
- 已整理本地剩余证据矩阵 `.release/v1.9-ui-acceptance-20260913/acceptance-matrix/RESULT.md`，避免把早期阶段的“未完成”套到后续已实际完成的项目。下一次真实下载窗口验收可优先选择已批准的 `genie-language-data`（13,182,009 压缩字节，约 12.57 MiB）；源码确认资源中心仅启动所选档位，不自动拉取其引擎依赖。本轮未发出下载请求，该方案尚非下载验收结果。

### 同日实际安装、模型导入与尺寸验收

- 在真实资源中心搜索“词典”并选择基础模型，实际点击安装 Genie 中英文发音词典（12.6 MiB）。界面从“正在处理”变为“已安装”，统计为 10 项资源、1 项已安装，其余引擎仍未安装。关闭资源窗口再重开，新窗口仍显示 1 项已安装。下载完成过快，没有在下载进行中完成关闭动作，因此只确认实际安装和完成后重开保持。该词典保留在本机。
- 创建独立本地角色“Live2D 验收”，通过原生文件选择器导入已预检的凯尔希 `.model3.json`；实际提示导入 14 项素材、约 1589 KiB，能力核对为 9 个动作组、0 个表情、未声明音频口型。没有覆盖小猫角色或为当前模型猜测动作、情绪映射。
- 此次纯 Live2D 验收显式将已有本地 Core 单文件临时放入构建目录，源与目标 SHA-256 均为 `8741f739779b5d5210872bd3d7d99f0f1e56e6c87409e7d26d6bb4b80aa1ef47`。关闭设置后，凯尔希模型实际完整显示在角色区域；随后重载 Renderer 页面，已导入角色及模型恢复显示，聊天和时钟仍可用。这是受控本地 Core 条件下的基本渲染及页面重载验收，不覆盖安装包 Core 供应、主进程重启、指定 GPU/软件渲染、动作映射或十通道表达。
- 完成后切回“小猫”，恢复 VTS 启用与原有鼠标追踪启用，关闭设置后实际看到 VTS 模型恢复、时钟继续更新。临时 Core 在核对绝对路径与哈希后仅删除该单文件，确认目标已不存在，源资产未改动。独立“Live2D 验收”角色及其导入模型保留在本机角色库；移除临时 Core 后，后续再次使用该验收角色的纯 Live2D 仍需准备运行时前提。
- 使用应用自身“桌宠大小”滑块，从 87%（实际窗口截图 957×661）调至最小 65%（715×494）。小窗口下设置保存/关闭按钮、聊天输入/发送、底部设置和小组件入口仍可见，媒体与时钟排列正常；关闭设置后 VTS 模型恢复显示。随后恢复 87%，实际设置值已复核。该项仅覆盖这两个实际窗口尺寸和当前布局，不等同 Windows DPI、多屏、拖动、所有设置分类或点击穿透验收。
- 新增隔离 Electron 43.4.1 整链验证通过：真实 ResourceCenterWindow、沙箱 Preload、Renderer、IPC sender 校验和下载管理器，对本地 HTTP 限速合成 ZIP 发起安装。窗口 1 在 8,192/262,507 字节时销毁，窗口数为 0 期间仍增长到 20,480 字节；窗口 2 读取到 downloading/24,576 字节，最终 Main 与 Renderer 卡片均为 ready。安装后的 262,144 字节文件与合成模型逐字节一致，资源请求全程仅 1 次。窗口隐藏、信任表仅在测试进程替换、数据在隔离目录，未操作用户实例；不能将该结果写作真实用户窗口下载中关闭的视觉验收。证据与重跑命令位于 `.release/v1.9-ui-acceptance-20260913/resource-window-electron/`，测试进程已退出。

### 同日试听控制与原生窗口补验

- 在真实资源窗口操作 Mika 试听，再点击“停止试听”，后续观察按钮恢复为“试听一句”；另一次从“停止试听”状态切到引擎分类，返回音色分类后仍为“试听一句”。随后从试听状态关闭资源窗口，窗口列表确认该窗口已消失，主设置窗口仍可用；重新打开成功。以上是实际交互及界面状态记录，短样本可能在操作间隙自然结束，没有系统音频采集或逐项耳听证据，不据此确认主动截断时刻和无残留声音。
- 将资源窗口最大化到实际 1920×1032，页面内容居中、卡片与操作按钮排列正常。在同屏可见两个试听按钮时从 Mika 切到菲比，实际观察到 Mika 恢复“试听一句”、菲比变为“停止试听”；随后点击停止，复看两个按钮均为“试听一句”。完成后还原到 1066×773、原位置（427,126），音色分类和滚动区域保留，再关闭资源窗口并返回桌宠。没有新装音色或引擎。
- 本轮使用原生控制工具尝试桌宠设置标题及顶部拖动区，窗口位置均保持（855,91）；随后对资源窗口标准 Windows 标题栏作同工具对照，位置也保持（427,126）。只读代码核对未发现禁止移动、move 事件拉回位置或全窗忽略鼠标的设置。该工具路径未取得有效原生拖动，不能记录为拖动通过，也不足以认定桌宠拖动缺陷；没有据此修改 Main 或 CSS。
- “点击穿透”分为两个边界：时钟及空白叠层的 `pointer-events: none` 和无点击处理只证明 Renderer 内不截获指针事件，媒体卡片单独恢复 `pointer-events: auto`。时钟是主窗口内的普通 DOM，不是独立原生窗口；当前源码未发现 Electron 主窗口的鼠标透传实现，故“从时钟区域点击到 Windows 桌面或其他应用”未验证且缺少相应实现证据。VTS Spout 辅助图像窗口的 `HTTRANSPARENT`/`WS_EX_TRANSPARENT` 属于另一窗口，不能代替时钟的证明。没有在本轮擅自新增跨窗口交互行为。
- 并行尝试时钟跨真实隔离 Electron 进程退出重启验证，未取得通过证据。首轮探针进入 Renderer 后暴露自身 Vite 路由接线问题；修正后独立 Electron 在探针入口前退出（4294967295/0xFFFFFFFF），未进入安装、启停或重启状态断言。经有限启动方式核对后停止重复尝试，未强杀或退出用户主进程；该启动环境问题不记为产品持久化失败。脚本和准确边界保存在 `.release/v1.9-ui-acceptance-20260913/clock-process-restart/RESULT.md`。原有服务重建证据仍不升级为主进程重启验收。

### 同日运行时修复与向导收尾

- 已把 Cubism Core 与模型目录分开：应用只加载固定 `runtime/cubism/live2dcubismcore.min.js`，不采用角色清单选择脚本。新增显式 `pnpm build:renderer:local-core` 本地模式，只校验并输出已知单文件；普通构建和 CI 仍不复制 Core 或私人模型。缺失、内容变化和输出隔离的构建回归先出现 3 项失败，修复后通过；真实配置的模式选择及本机开发路由另有回归。来源与公开分发的未决条件详见 [本地 Live2D 运行时](LIVE2D_LOCAL_RUNTIME.md)，不能把本机内容指纹当作官方来源证明。
- Core 加载器合并并发请求，区分不可用、无效和 30 秒超时，失败后移除脚本与计时器并允许重试。错误界面显示“Live2D 运行时不可用”，说明已导入模型保留，不再让用户为运行时问题反复导入模型。真实隔离浏览器验证普通模式缺失提示和重试入口、本地模式真实 Core 脚本加载均通过，证据为 `.release/v1.9-ui-acceptance-20260913/core-runtime-browser-result.json`。
- 实际执行本地 Core 构建，输出文件 SHA-256 与预期本地副本一致。重载桌宠页面、切到已存在的“Live2D 验收”角色并启用纯 Live2D 后，凯尔希模型实际显示；本次没有手工向构建目录复制文件。完成后恢复小猫、VTS、鼠标追踪和时钟。当前本地 Renderer 输出保留显式构建的 Core，后续普通 Renderer 构建会清理它；未制作安装包或发布。
- 首次向导新增真实浏览器 11 场景通过：完整生产页面和真实 HTMLAudioElement 解码四个捆绑 WAV，未安装资源时可播且未调用下载/合成；主动停止、切换音色及生产“下一步”离页均暂停旧音频；组件未就绪的禁用/错误/跳过，以及 Tab/方向键/Enter/空格路径均通过。实际页面发现“可选日语朗读”与中英音色不一致，标题断言先红后绿，已修为“可选中文、日语或英语朗读”。使用内存 Setup API 与状态，未重置用户向导；可访问树不等于真实读屏，媒体状态不等于扬声器耳听。证据在 `.release/v1.9-ui-acceptance-20260913/setup-acceptance/`。
- Astra 同文本样本新增可重跑的有效能量起点、低能量/精确零段与候选传输边界分析，原始音频哈希保持不变。结果和限制集中于 [Astra 研究](V1_9_ASTRA_RESEARCH.md)，未用指标替代连续听感或扩大为正式引擎接入。
- 本次全部代码改动后完整 `pnpm verify` 实际退出 0：158 个测试文件、858 项测试，lint、格式、两套类型检查、全部构建、沙箱 Preload、Electron 原生语音、SQLite 与首次/已有配置启动检查通过。日志 `.release/v1.9-ui-acceptance-20260913/verify-runtime-closeout.log`；随后显式本地 Core 构建通过并完成上述实机复看。时钟独立 3 PID 重启探针在此后只重试一轮仍在入口前退出，仍不记为持久化失败或通过。所有源码保留未提交状态，未推送或发布。

### 同日发布前的通用动作验证

- 原生手势加入实际参数后备，独立审查发现的失焦交互锁和旧动作回调竞态均修复并取得回归证据。生产代码冻结后，真实 Cubism、生产 Pixi 驱动和固定公开许可模型共 7 个场景通过：点头/摇头及重复、状态/交互/口型取消。1573 次全部 54 个参数的保存/恢复周期误差均为 0，手势没有写入两个实际嘴部参数。证据 `.release/v1.9.0/gesture-browser/RESULT.md`；这是运行时参数证据，不等同真人自然度、用户实例完整交互或全部模型验收。
- 最终完整验证、成品内容、成品隔离启动与 ASR、提交和发布结果集中于交付记录，历史段落中的“尚未提交/打包”仅描述当时状态。时钟 3 PID 探针后续有界启动差异核查仍未取得入口记录，因此保留未验收；没有把环境启动失败记为产品持久化缺陷。

## 验收项目

- 独立资源中心和任务栏图标：多次打开、最小化、恢复及关闭后的下载状态。
- 100%／125%／150% DPI、多屏切换、窄屏及窗口拖动时的面板、卡片和按钮布局。
- 内嵌 Live2D 的独立 GPU 与强制软件渲染；下载、解压与故障恢复期间的实际帧率观感。
- VTube Studio 人工授权、Spout2 输出、模型表情和口型的实际桌面效果。
- 时钟安装后默认关闭；启用后时间、日期、时区正确，每秒更新；关闭后消失，重启保留选择。检查其与输入/媒体叠层的间距；分别验证 Renderer 内时钟不截获事件、媒体按钮可交互，以及跨 Electron 主窗口点击 Windows 桌面（后者当前缺少实现证据，不能由 CSS 透传推定支持）。
- VTS 与原生 Live2D 的混合情绪、弱强度、连续打断与 8 秒恢复；确认口型、动作和固定参数不被抢占。
- ViewerEX 已填写动作映射的单项预览、候选轮换、1.5 秒待机衔接、8 秒表达恢复；手动添加表情后留意全局清除接口的影响。
- Mika 多句日语及不同情绪的自然度；检查停顿降噪是否影响特别轻的发音。保留原音频、处理后对照与新安装试听。
- 向导三种 Genie 音色的实际试听、停止、切换与离页行为；组件未就绪提示，以及纯键盘操作和读屏体验。
- 下载任何模型前，资源中心和向导里的四种音色均可点击“试听一句”；切换、停止、关闭页后无残留声音。37 示例的 was 曾被 ASR 识别为 is，需耳听确认而非仅凭识别结果判断。
- AstraTTS 同文本完整生成与流式的连续听感；尤其复查开头词内卡顿和分块接缝。研究样本满意后，正式接入仍需独立验收。
- 需要真实账户的在线 Fish Audio／OpenAI TTS 与中文转日语服务，使用者自行配置凭据后再做实际试听。

传统 OneDrive VITS/MoeGoe 音色保持暂缓，并不属于本轮已交付组件。后续扩展单独评估兼容性和许可。
