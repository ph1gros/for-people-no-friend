# V1.8 接手记录

日期：2026-09-04。接手任务：`FPNF V1.8 目标与开发启动`。

## 最新产物：v1.7.1 本地 Windows 包（2026-09-04）

用户要求将截至当前的改动打为 v1.7.1。已将 package.json 及版本断言更新为 1.7.1，
保持既有修改，执行本地 `pnpm package:win`；没有提交、推送、标签或上传发布。

- ZIP：`release/v1.7.1-artifacts/FPNF-v1.7.1-Windows-x64.zip`。
- ZIP 大小 145,507,637 字节（138.77 MiB）；解压后含使用说明共 49 个文件，
  354,089,251 字节（337.69 MiB）。同目录保留 FPNF 文件夹、SHA256SUMS.txt 和 package-info.json。
- SHA256：`415F9D1677165E2B2F8F0FAB19A844D587D01C5AB6D5566ADC1EE92CE095204F`。
- EXE 内部版本及 ASAR 元数据均为 1.7.1；成品未签名。主程序保留 ASR 原生引擎，
  不含语音模型、Python 环境或已检查的私密角色/音色资产，生产语音下载继续禁用。
- 沿用下节刚通过的 574 项整仓验证；版本变更后另跑打包配置/资源排除 4 项测试、格式及构建。
  成品 ASR 加载外部既有测试模型后准确转写合成音频；未启动日常桌宠 UI。
- 最终 ZIP 用 UTF-8 文件名重新生成，逐一核对全部 49 个文件的解压内容 SHA256 与目录原件一致。
  真实窗口、Live2D 与完整 TTS 人工验收仍未完成。

## 最新补充：DOM 工厂提取验收（2026-09-04）

- Claude 写回 `chat/elements.ts`、265 处 `el()` 声明及 5 项工厂测试；当前
  `chat-controller.ts` 实测 5392 行。未继续提取语义组件或拆分剩余面板。
- 本轮初检发现 `elements.ts` 格式不符、仅作类型使用的 `WRITABLE_KEYS` 触发 lint，
  以及 11 项旧 wiring 测试因匹配逐行赋值源码而失败。
- 将属性键数组改为纯类型联合并格式化；核对 18 条受影响断言的值后，改用 TypeScript AST
  读取对应 DOM 属性，兼容 `el()` 与独立赋值，保留原先预期值及正反向检查。
- 加强 falsy 用例，确认空字符串、false、0 确实发生赋值，而非仅与假 DOM 默认值相同。
- 额外编译检查通过：合法 input/select/form 属性与返回类型成立；错误标签属性、innerHTML
  和错误属性值类型按预期编译失败。没有使用真实用户配置或启动日常桌宠界面。
- 最新 `pnpm verify` 完整通过：116 个测试文件、574 项测试；lint、格式、两套类型检查、
  构建、Preload 沙箱、sherpa addon 和 SQLite backup 烟雾全部通过。验证日志位于
  `.release/dom-extraction/verify.log`（忽略的本地产物）。

未独立复核 Claude 所述的改前 339 个元素机械比对，因为本轮没有对应改前备份；
自动检查通过不代表真实 UI 目视验收。本轮没有打包、提交、推送或发布。

## Claude 写回后的运行时复核（2026-09-04）

在保留 Claude 写回的残留清理、清单失败重试与 PowerShell 64 KiB 缓冲修改后，补齐以下边界：

- 清单重试成功会清除旧错误；失败缓存按请求身份清除，避免旧请求影响新请求。
- 清理仅匹配固定档位/目标与 UUID。备份可能是安装中断后仅存的旧副本，只有替换目录
  必需文件完整、当前激活记录匹配内置信任记录时才删除；否则保留，不自动恢复或执行。
- 清理与手动安装共用一次性启动屏障，未配置源也清理 staging，`.downloads` 续传分片保留。
- 手动开始/继续前刷新计费状态；并发检测合并，检测期间取消/暂停/退出不会在等待结束后启动。
- 网络检测区分计费、非计费、未知。自动下载仅在明确非计费时启动；主动下载可在计费或未知
  网络继续，设置页和下载细条显示流量提示。未实现下载过程中的持续切网监测。
- 定向回归先复现旧错误残留、备份误删、前缀匹配过宽及计费检测缺口，再修正通过。
- 最新 `pnpm verify` 完整通过：115 个测试文件、569 项测试；lint、格式、两套类型检查、
  构建、Preload 沙箱、Electron 43.4.1 / Node 24.18.1 下 sherpa 与 SQLite backup 烟雾全部通过。
  初次受限环境仍在 `.tmp-spout2/ziglang/bin` 的格式扫描遇到 EPERM，经审批正常用户权限
  重跑原命令成功，未修改目录权限或检查规则。

生产两档信任记录仍为 `null`。本轮没有下载/执行远程 Python，没有重打包、提交、推送或发布；
尚未做真实热点切换和桌面 UI 目视验收。下文 551 项测试与成品包记录保留为前一轮证据，
前一轮成品包不包含本节源码修改。

## 工作区与既有进度

- 继续使用 `C:\ai_deskpet` 当前 `main`，基线提交 `fd81cfd`。
- 保留原任务全部未提交修改；没有创建工作树、提交、推送、标签或发布。
- 原任务最后的完整验证记录为 112 个测试文件、536 项测试；它不是本轮验证结果。
- V1.8 布局、组件避让、显示方式切换的代码及自动回归已存在，真实模型目视验收仍待做。
- T2 Node ASR、T3 干净 TTS 运行时及 T8 打包裁剪已有实现；不把原任务报告当作本轮实机复验。

## 本轮：修正 T6 信任依据

- 清单升为 schema 2，仅接受 `id/version/urls`。拒绝旧 schema、远程哈希/大小/目标、
  未知档位及多余字段。BERT 并入 `voice-runtime`，只保留它与 `speech-input` 两档。
- SHA256、压缩大小、解压上限、文件数和目标由源码内的冻结记录决定；安装入口再次
  解析可信记录，不接受传入对象里的替换哈希、体积或目标。
- 生产记录暂为 `null`：没有获确认的最终资产、实测元数据和公开分发许可，不能编造值。
  无可信记录时不发起资产下载，并在状态中说明不可用。
- 下载前检查磁盘空间；顺序续传与分段响应按内置体积核验；哈希失败删除坏包并结束
  本次操作，不把内容完整性失败当作网络故障循环切换镜像。
- 解压按内置限额执行，等待所有写入结束后再清理失败 staging。
- 新安装写 schema 2 激活记录。状态恢复及用户目录 Python 启动均检查记录与内置信任
  依据匹配，旧记录或未验证的下载目录不再被优先启用。
- 测试使用临时目录、假 ZIP、mock 网络或本地 HTTP；未下载或执行远程 Python，
  未读取真实 API Key 或私人对话。

信任范围：内置哈希约束远程源，依赖应用本体可信；本机文件被攻击者修改不在此方案
能独立防御的范围内。激活记录不是数字签名。

## 验证记录

- 先观察旧代码接受远程哈希、安装接口接受替换哈希、旧下载目录被直接选中、
  续传接受错误总大小的失败回归，再修正到通过。
- 原有本地 HTTP 续传、四段并发、暂停/取消、Zip Slip 回归保留。
- 新增远程额外字段、版本不匹配、无生产记录、篡改 ZIP、磁盘不足、解压限额、
  全部镜像失败及旧激活记录的回归。
- 整仓 `pnpm verify` 通过：114 个测试文件、551 项测试；ESLint、Prettier、两套
  TypeScript 检查、Electron/Renderer 构建、Preload 沙箱检查，以及 Electron 43.4.1 /
  Node 24.18.1 下 sherpa addon 和 SQLite backup 实际烟雾均通过。
- 初次格式扫描受沙箱对既有 `.tmp-spout2` 目录的读取权限限制；正常用户权限下完成
  同一验证流程。三份旧任务/交接文档仅做格式规范，并给交接首页增加本记录入口。
- 下节补充本地成品包原生模块验收；真实窗口、Live2D 模型与 Genie 服务验收仍未执行。

## R1：本地成品包检查（2026-09-04）

按用户指定的四份文档顺序复核后，执行第二轮明确要求的本地 `pnpm package:win`。
输出仍在 `release/win-unpacked/`，没有上传、提交或发布。

- 打包前同路径本机旧目录：406,075,593 字节、79 个文件。
- 本次目录：354,088,623 字节、48 个文件，减少 51,986,970 字节（约 49.6 MiB）。
  这是本机旧目录与当前构建的比较，不是保持其他变量一致的 DLL 裁剪实验，也不是 ZIP 大小。
- ASAR 目录共 3,065 项。sherpa 的 `.node` 和 4 个 DLL 均标记为 unpacked，
  实际加载验证通过。没有发现 `.cmo3`、已检查的黑猫/凯尔希路径或 `voice/ireina` 资产路径。
- `locales/` 仅有 `en-US.pak` 与 `zh-CN.pak`。
- `dxcompiler.dll`、`dxil.dll` 不存在；`d3dcompiler_47.dll`、`libGLESv2.dll`、
  `vk_swiftshader.dll`、`vulkan-1.dll` 仍存在。可执行文件状态为 `NotSigned`。
- 新增 `scripts/verify-packaged-sherpa.cjs`，通过**成品可执行文件的 Node 模式**加载
  `app.asar` 中的适配器和 unpacked 原生依赖，不启动日常桌宠 UI，也不读取真实用户配置。
- 使用既有合成 WAV 与既有 SenseVoice 模型，在 Electron 43.4.1 / Node 24.18.1 下
  得到完全相同的预期转写：`你好，欢迎使用本地语音识别。今天天气很好。`
- 缺失模型时适配器返回不可用已验证；这不是“真实聊天窗口始终可用”的 UI 验收。

复验命令：

```powershell
node scripts/verify-packaged-sherpa.cjs release/win-unpacked release/speech-slim-prototype/speech-input-runtime/models/sensevoice .release/t2-feasibility/same-audio.wav
```

首次打包在沙箱中因 pnpm 依赖数据库不可读而失败（`unable to open database file`）。
同一依赖清单命令在正常用户权限下只读验证成功后，同一本地打包命令成功；未改文件权限、
安全规则或包管理器配置。

R1 仍未整体完成：未打开成品主窗口验收正常 GPU/软件渲染 Live2D；未把私密 TTS 权重
放入主包，未做成品 TTS CPU/DirectML 及真实分档 ZIP 体积验收。生产信任记录仍为 `null`。

## 后续顺序与未完成边界

1. R1：成品 sherpa 实际转写和静态裁剪检查已通过；GPU/软件渲染、打包 TTS、分档 ZIP 验收待做。
2. R2：非模态首次下载告知、剩余时间与“不再自动下载”的持久化。两档与磁盘检查已改。
3. R3：采用完整下载并校验后解压；大资产峰值占用仍须实测。
4. R4：本轮为自动故障回归；真实断网、进程退出、磁盘耗尽、网络计费状态切换及
   同时保持 UI 流畅仍未验收，不能用假文件测试宣称通过。
5. R5/T7：memory-panel、settings-provider、settings-speech、settings-vtube、
   settings-character 五个面板仍未拆分。
6. R6：Genie-TTS 端到端连接、参考音频、取消和听感验收。
7. R7：最终语音资产、再分发许可和托管地址需要作者决定；在此之前保持生产下载禁用。

规则继续有效：不改 ONNX 权重，不上传私密资产，不操作真实凭据，不提交/推送/发布。
本记录不授予新权限，后续动作仍以用户授权和项目规则为准。
