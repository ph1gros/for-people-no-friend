# FPNF 改进任务 · 第二轮

> 接续 `IMPROVEMENT_TASKS.md`。第一轮 14 个任务已全部推进，其中 10 个完成、4 个部分完成。
> 本文件只写**第一轮之后新产生的信息**和**剩余工作**，不重复第一轮已完成的内容。

---

## 0. 先修正一条上一轮写错的约束

### G8 的措辞挡住了必要的验证 —— 现予澄清

第一轮 `IMPROVEMENT_TASKS.md` 的 G8 写的是"不得提交、推送、发布、**打包**、改写历史或修改仓库可见性"，
但附录 C 又要求"T2 / T3 / T6 / T8 必须跑 `pnpm package:win`"。**这两条是矛盾的**，
上一轮执行者选择了保守的一边（正确的选择），代价是 T2 的 addon 打包加载、T8 的裁剪效果都没验证。

**澄清后的 G8**：

> 禁止的是**对外发布**：`git commit` / `git push` / 创建 Release / 打 tag / 改写历史 / 修改仓库可见性 / 上传任何产物。
> **允许并且要求**：在本地运行 `pnpm package:win` 产出 `release/win-unpacked/`，
> 用它做启动与功能验收，验收完可以删掉产物。这是本地验证，不是发布。

其余全局约束（G1–G7）不变。特别重申 **G1：不得改动任何 ONNX 模型权重**。

---

## 1. 一个实测结论改变了 T6 的设计

### 发现

第一轮实测确认：**删除日语 BERT 后 Style-Bert-VITS2 直接失败，不会退化到平语调。**

这推翻了第一轮 T6 里"BERT 可作为可选第二档"的假设（那条当时就标注为"需先实测确认"，现在测出来了）。

### 后果：分档必须重算

|                    | 第一轮设想            | 实测后修正                       |
| ------------------ | --------------------- | -------------------------------- |
| 第一档（静默预下） | Python + TTS ≈ 240 MB | **Python + TTS + BERT ≈ 490 MB** |
| 第二档             | BERT ≈ 250 MB         | _（不存在，已并入第一档）_       |
| 末档（点麦克风时） | ASR ≈ 225 MB          | ASR ≈ 225 MB（不变）             |

体积依据：T3 重建后 voice-runtime 未压缩 801.3 MiB（BERT 408 + TTS 249 + 干净 Python ≈ 144），
按 ONNX 压缩率不高、Python 环境压缩率高估算，压缩后约 **490 MB**。
**实际数字以打包后为准，见 R1。**

### 后果：下载时间也要重算

| 包大小             | 0.5 MB/s | 2 MB/s | 10 MB/s | 30 MB/s |
| ------------------ | -------- | ------ | ------- | ------- |
| 490 MB（新第一档） | 16 分钟  | 4 分钟 | 50 秒   | 17 秒   |
| 225 MB（ASR 档）   | 8 分钟   | 2 分钟 | 23 秒   | 8 秒    |

分块并发下载通常能把慢速链路的实际速度拉高 2–4 倍，但 490 MB 已经不是
"用户察觉不到"的量级了。**"静默预下"的策略需要相应调整，见 R2。**

---

## 2. 剩余任务

| 编号 | 标题                                        | 优先级     | 预估     | 前置      |
| ---- | ------------------------------------------- | ---------- | -------- | --------- |
| R1   | 本地打包验收（T2 / T3 / T6 / T8 的收尾）    | P0         | 半天     | G8 已澄清 |
| R2   | 修正 T6 分档与预下载策略 + 磁盘空间检查     | P0         | 半天     | R1        |
| R3   | 边下边解压，或明确接受峰值磁盘占用          | P1         | 半天     | R2        |
| R4   | T6 故障注入验收（断网 / 磁盘满 / 中途退出） | P1         | 半天     | R2        |
| R5   | T7 剩余五个面板                             | P1         | 持续     | —         |
| R6   | Genie-TTS 端到端听感验收                    | P2         | 1 小时   | —         |
| R7   | 托管与清单：作者需先决策的事项              | P0（阻塞） | 作者决定 | —         |

---

## R1 · 本地打包验收

**优先级** P0 · **预估** 半天 · **前置** G8 已澄清

第一轮有三处验收因为 G8 措辞而未执行，现在补上。

### 步骤

```bash
pnpm verify          # 先确认源码层面干净
pnpm package:win     # 本地产出 release/win-unpacked/
```

然后**实际运行** `release/win-unpacked/For People No Friend.exe`，逐项验收：

### 验收清单

**T2 收尾 —— 原生 addon 在打包产物中能加载**

- [ ] 打包后 ASR 可用，用与开发环境相同的测试音频得到相同转写结果
- [ ] 确认 `sherpa-onnx-node` 及其平台包确实落在 `resources/app.asar.unpacked/` 下
      而非被打进 asar（asar 内的 `.node` 文件无法 `dlopen`）
- [ ] 检查 `asarUnpack` 的 glob 是否命中了**全部**需要的文件
      （addon 主体 + 平台二进制包，两者可能是不同的 npm 包）
- [ ] 模型文件缺失时安全降级，文字聊天不受影响

**T8 收尾 —— 裁剪效果与副作用**

- [ ] 记录裁剪前后 `win-unpacked/` 的实际体积对比
- [ ] 确认 `dxcompiler.dll` / `dxil.dll` 确实不在产物中
- [ ] 确认 `locales/` 只剩 zh-CN 与 en-US
- [ ] **在有独立 GPU 和无独立 GPU（或强制软渲染）两种情况下**分别启动，
      确认 Live2D 都能正常显示 —— 这是删 DirectX 相关 DLL 后最需要验的一条
- [ ] 确认 `.cmo3` 不在产物中

**T3 收尾 —— 打包后的 TTS**

- [ ] 打包产物中的 voice-runtime 能启动并生成 WAV
- [ ] DirectML 与 CPU 两条 provider 路径都验证（可临时置 `force_cpu_only`）

**T6 收尾 —— 真实体积**

- [ ] 按新分档实际压缩出第一档与 ASR 档的 zip，**记录真实体积**，
      回填到本文件第 1 节的表格里替换估算值

### 完成后

产物可以删除。**不要 commit `release/` 下的任何东西**（`.gitignore` 已忽略，确认它仍然生效）。

---

## R2 · 修正 T6 分档与预下载策略

**优先级** P0 · **预估** 半天 · **前置** R1（需要真实体积）

### 改动一：合并档位

- 删除独立的"BERT 第二档"，把 BERT 并入第一档
- 清单 JSON 的结构相应简化为两档：`voice`（TTS + BERT + 运行时）与 `asr`
- 版本化清单是无额外字段的严格校验，**改结构时同步更新清单版本号与校验逻辑**
- 相关测试同步更新

### 改动二：490 MB 不适合完全静默

第一轮写的是"角落一条不打断的细条，默认就在下"。那是按 240 MB 设计的。
490 MB 需要把告知做得更实在一点，但**仍然不要弹模态对话框**：

- 细条上**明确显示体积和预计剩余时间**："正在准备本地语音 · 已下载 120 / 490 MB · 约 4 分钟 · 暂停"
- 首次启动时这条细条**默认展开可见几秒**再收成细线，确保用户看见过
- "暂停"之外增加"不再自动下载"，记住选择
- 计费网络检测保持现有行为（默认暂停）

### 改动三：新增磁盘空间检查（这是 490 MB 带来的新问题）

当前实现是"先完整下载 zip + SHA 校验，再流式解压"，所以峰值磁盘占用是：

```
490 MB（zip） + 801 MB（解压后） ≈ 1.3 GB
```

**开始下载前必须检查目标盘可用空间**，建议阈值 = 声明解压体积 × 1.3 + 500 MB 余量。
空间不足时给出可读的中文提示并停止，而不是下到一半写失败。

清单里应当同时声明 `compressedBytes` 与 `extractedBytes` 两个字段，
让检查在下载**开始前**就能做，而不是下完才发现装不下。

### 验收标准

- [ ] 分档结构改为两档，清单版本号已升，旧清单被拒绝
- [ ] 细条显示真实体积与剩余时间，首次启动时确实可见
- [ ] "不再自动下载"的选择在重启后仍然生效
- [ ] 磁盘空间不足时在**下载开始前**就给出提示并停止
- [ ] 清单缺少 `extractedBytes` 字段时被严格校验拒绝
- [ ] `pnpm verify` 通过

---

## R3 · 边下边解压，或明确接受峰值占用

**优先级** P1 · **预估** 半天

第一轮任务书写了"边下边解压（项目已依赖 `fflate`）"，实际实现是先下完再解压。
这不算偏离——**分段并发下载与流式解压天然冲突**：Range 分段是乱序到达的，
而 zip 的流式解压需要顺序字节流。

### 两条路，选一条

**路线 A：接受现状，把峰值占用管理好**

- R2 的磁盘空间检查已经覆盖了主要风险
- 优点：实现简单，SHA 校验在解压前完成，安全性更好（不会把未校验的内容写进目标目录）
- **推荐这条**

**路线 B：真正的边下边解压**

- 需要放弃分段并发（改回单线程顺序下载），或者按 zip 条目边界切分段
- SHA 校验只能在最后做，意味着可能已经把未校验内容写进 staging
- 收益仅是省下峰值磁盘的一半，代价是失去并发下载的速度优势

### 决策记录

无论选哪条，**在 `docs/SPEECH_ASSET_DOWNLOADS.md` 里写清楚为什么**，
避免将来有人以为这是遗漏。如果选路线 A，把第一轮任务书里"边下边解压"那条标注为已重新评估。

---

## R4 · T6 故障注入验收

**优先级** P1 · **预估** 半天

下载器已经有暂停/继续/取消/重启恢复的逻辑和测试，但**没有做过真实的桌面故障验收**。
以下场景需要在真机上实际制造：

- [ ] **下载到一半拔网线**：恢复网络后能续传，不从头开始
- [ ] **下载到一半强杀进程**（任务管理器结束进程）：重启后能从断点继续
- [ ] **目标盘写满**：给出可读提示，不留下损坏的半成品
- [ ] **SHA256 不匹配**（手工改一个字节）：资产被删除，提示明确
- [ ] **清单 URL 全部不可达**：安全失败，文字聊天不受影响
- [ ] **staging 目录被外部删除**后继续下载：能正确重建或重来
- [ ] **下载中切换到计费网络**：按策略暂停
- [ ] 全过程中桌宠对话与 Live2D 渲染**始终不卡顿**（G5）

每一项记录实际观察到的行为。发现与预期不符的，先记录再修。

---

## R5 · T7 剩余五个面板

**优先级** P1 · **预估** 持续

已完成：`timeline.ts`、`composer.ts`、语音资产面板。
`chat-controller.ts` 从 5824 行降到 5720 行 —— 说明搬出去的主要是渲染与事件绑定，
**状态仍留在主控制器的闭包里**，这是预期中的中间态。

剩余五块，建议顺序（依赖最少的先搬）：

1. `memory-panel.ts` —— 记忆列表与候选，边界最清晰
2. `settings-provider.ts` —— 模型提供方设置
3. `settings-speech.ts` —— 语音设置（语音资产子面板已独立，这里是其余部分）
4. `settings-vtube.ts` —— VTube Studio / ViewerEx
5. `settings-character.ts` —— 角色档案与调研

### 每块的验收

- [ ] 有独立测试文件
- [ ] `mount` 返回的 dispose 完整解绑该模块所有监听与定时器
- [ ] DOM 结构与 class 名与搬动前完全一致（`styles.css` 依赖它们）
- [ ] `chat-controller.ts` 行数相应减少
- [ ] `pnpm verify` 通过

### 一个中期目标

五块都搬完后，`chat-controller.ts` 应该只剩下"状态容器 + 各面板的装配与协调"。
到那时再考虑把闭包状态提成显式的 store 对象 —— **现在不要做**，会和面板搬迁互相干扰。

**仍然：一次一个面板，单独提交。**

---

## R6 · Genie-TTS 端到端听感验收

**优先级** P2 · **预估** 1 小时

构建与测试全绿，但没有启动真实 Genie 服务、加载参考音频做端到端验收。
这是一条容易在重构中被静默破坏、而单元测试发现不了的路径。

- [ ] 启动真实 Genie-TTS 服务
- [ ] 通过设置页配置并连接
- [ ] 加载参考音频，实际生成一段语音并试听
- [ ] 确认取消（打断）行为正常
- [ ] 确认服务不可达时安全降级

同理，`fish-audio` 与 OpenAI 兼容 TTS 若有条件也各跑一次。

---

## R7 · 托管与清单：需要作者先决策

**优先级** P0（阻塞 T6 收尾）· **需要人做决定，不是编码任务**

上一轮正确地**没有虚构下载地址、没有把私密 ZIP 放进 Git 或 Release**。
这是对的判断，本轮继续保持。以下是需要作者本人决定的事：

### 决策一：Ireina 音色的再分发许可

这是最硬的一条。在拿到明确的公开再分发许可之前：

- 不要把音色权重放进任何公开渠道
- 可以先用**一个占位的、有明确许可的日语音色**跑通整条下载链路，
  把音色替换留作最后一步

### 决策二：托管在哪

| 选项           | 适合                                                                              | 注意                                         |
| -------------- | --------------------------------------------------------------------------------- | -------------------------------------------- |
| HuggingFace    | 主源。本来就是分发模型的地方，有 CDN、支持 Range，国内有 `hf-mirror.com` 可作回退 | 私有 repo 需要 token，公开 repo 要先解决许可 |
| Cloudflare R2  | 备源。出口流量免费，490 MB × N 个用户不产生账单                                   | 需要自己配域名与 CORS                        |
| GitHub Release | 兜底。单文件 2 GB 限制，490 MB 没问题                                             | 大陆裸连速度不可预测                         |

### 决策三：清单托管在哪

清单 JSON 应当独立于资产托管，且可以在**不发新版本**的前提下更新。
GitHub Pages、R2、或仓库的 raw 文件都可以，但要能加 CDN 缓存且缓存时间不要太长。

### 在决策落地之前

- 开发模式允许 localhost HTTP 的设计是对的，继续保留
- 可以在本机跑一个静态文件服务器做完整的下载链路验收，**这不需要公网站点**
- 正式包强制 HTTPS 的约束不要放宽

---

## 附录 · 第一轮结论中被实测修正的部分

| 第一轮的说法                            | 实测结论                                               |
| --------------------------------------- | ------------------------------------------------------ |
| "BERT 只影响韵律，缺失时会退化到平语调" | **错。缺 BERT 直接失败。** BERT 必须在第一档           |
| "第一档约 240 MB"                       | **约 490 MB**                                          |
| "边下边解压（fflate 支持流式）"         | 与分段并发下载冲突，已重新评估，见 R3                  |
| G8 "不得打包"                           | 措辞错误，本地 `package:win` 是验证不是发布，见第 0 节 |

**未被推翻、继续有效的**：G1（不动模型权重）、G2（zip 免安装分发）、
G3–G7、以及第一轮附录 B 里"确认没问题、不要顺手优化"的全部条目。
